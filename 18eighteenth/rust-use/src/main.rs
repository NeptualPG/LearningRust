// main.rs
// use nested paths to clean up larger use lists


use std::collections::*;

use::std::io::Write;
// use::std::io::{self, Write};

// Different import ways
// use std::{cmp::Ordering, io};
use::std::cmp::Ordering;
use::std::io;